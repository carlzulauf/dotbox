# File acts to override any OS installed ~/.config/fish/config.fish file
#
# See ~/.config/fish/conf.d/ for most config
#
# System configs are loaded after ~/.config/fish/conf.d/, so aliases and other
#  entries are in this file, which is loaded after system configs.
